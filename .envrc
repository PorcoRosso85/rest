echo $(pwd)

echo -e "\n\n"
echo "source up $(pwd)/.."
source_up
export DJANGO_SETTINGS_MODULE=core.settings

echo -e "\n\n"
echo "source ./.venv/bin/activate"
source ./.venv/bin/activate

echo -e "\n\n"
echo "docker run --rm -p 5432:5432/tcp -e POSTGRES_PASSWORD=postgres -e POSTGRES_USER=postgres -e POSTGRES_DB=postgres postgres:latest"
docker run --rm -p 5432:5432/tcp -e POSTGRES_PASSWORD=postgres -e POSTGRES_USER=postgres -e POSTGRES_DB=postgres postgres:latest